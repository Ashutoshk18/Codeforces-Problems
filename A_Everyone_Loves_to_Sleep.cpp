#include<bits/stdc++.h>
using namespace std;

void solve()
{
    int n, h1, m1, H, M; 
    cin >> n >> H >> M; 
    for(int i = 1; i <= n; i++)
    {
        int sleep = H*60 + M; 
        cin >> h1 >> m1; 
        int alarm = h1*60 + m1; 
        
        
    }
}

int main()
{
    int test = 1; 
    cin >> test; 
    while(test--)
    {
        solve();
    }
}