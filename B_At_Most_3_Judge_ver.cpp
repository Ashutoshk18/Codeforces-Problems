#include<iostream>
#include<vector>
using namespace std; 
#define ll long long 
int main(){
    ll n, w; 
    cin >> n >> w; 
    vector<ll> mass(n); 
    for(int i = 0; i < n; i++){
        cin >> mass[i]; 
    }
    
}